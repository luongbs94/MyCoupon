<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ln.mycoupon">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="com.ln.app.MainApplication"
        android:allowBackup="true"
        android:excludeFromRecents="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- ======================= START ACTIVITY OF APP =============== -->
        <activity
            android:name=".FirstActivity"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme">

        </activity>
        <activity
            android:name=".shop.ShopMainActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".customer.CustomerMainActivity"
            android:excludeFromRecents="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".shop.ShopLoginActivity"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="stateHidden|adjustResize" />
        <activity
            android:name=".TestQRCode"
            android:label="@string/title_activity_test_qrcode"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddCouponActivity"
            android:label="@string/add_coupon"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".AddMessageActivity"
            android:label="@string/add_message"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".QRCodeActivity"
            android:label="@string/qrcode"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".customer.CouponCompanyOfClientActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".customer.CustomerLoginActivity" />

        <!-- ======================== END ACTIVITY OF APP =============== -->

        <service android:name="com.ln.firebase.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name="com.ln.firebase.MyFirebaseInstanceIDService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>

        <!-- ============== START FACEBOOK =============== -->
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <provider
            android:name="com.facebook.FacebookContentProvider"
            android:authorities="com.facebook.app.FacebookContentProvider1073363172685474"
            android:exported="true" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <!-- ===================== END FACEBOOK ================ -->
        <activity
            android:name="com.yongchun.library.view.ImageSelectorActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.yongchun.library.view.ImagePreviewActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.yongchun.library.view.ImageCropActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".PreviewImagesActivity" />

        <!-- CHECK NETWORK -->

        <receiver
            android:name="com.ln.broadcast.ConnectivityReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar.Theme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>